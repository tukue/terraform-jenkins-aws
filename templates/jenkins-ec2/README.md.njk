# ${{ values.name }}

Jenkins EC2 instance definition generated from the platform product Backstage template.

## Inputs

- `environment`: ${{ values.environment }}
- `instance_type`: ${{ values.instance_type }}
- `owner`: ${{ values.owner }}
- `enable_monitoring`: ${{ values.enable_monitoring }}
- `backup_retention`: ${{ values.backup_retention }}

## Next Steps

1. Review the generated Terraform.
2. Wire the module inputs to your environment-specific variables.
3. Merge through the platform delivery workflow.
