name        = "{{ name }}"
ami_id      = "{{ ami_id }}"
instance_type = "{{ instance_type }}"
subnet_id   = "{{ subnet_id }}"
key_name    = "{{ key_name }}"
environment = "{{ environment }}"
owner       = "{{ owner }}"
project     = "{{ project }}"
cost_center = "{{ cost_center }}"
aws_region  = "{{ aws_region }}"
